<Project>
  <PropertyGroup>
    <!-- Distro SDK packaging can omit prune package data for the TFM. -->
    <AllowMissingPrunePackageData>true</AllowMissingPrunePackageData>
  </PropertyGroup>
</Project>

# JJDevHub — React Native Mobile Client

React Native 0.84 + React Native Paper (Material Design). Public blog reader with pull-to-refresh and AsyncStorage cache.

App source: `JJDevHubMobile/` — see its [README](JJDevHubMobile/README.md) for setup instructions.

API URL is configured in the services layer. Keycloak OIDC for authentication (optional in dev).
